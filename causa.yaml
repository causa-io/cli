version: 1

workspace:
  name: causa

project:
  name: cli
  description: Exposes the command line interface for the Causa (`cs`) command.
  language: typescript
  type: package

causa:
  modules:
    '@causa/workspace-core': '>= 0.25.2'
    '@causa/workspace-typescript': '>= 0.15.2'

javascript:
  dependencies:
    check:
      allowlist: []
    update:
      packageTargets:
        '@types/node': minor
